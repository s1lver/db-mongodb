on:
  pull_request:
    paths-ignore:
      - 'docs/**'
      - 'README.md'
      - 'CHANGELOG.md'
      - '.gitignore'
      - '.gitattributes'
      - 'infection.json.dist'
      - 'psalm.xml'

  push:
    branches: [ 'master' ]
    paths-ignore:
      - 'docs/**'
      - 'README.md'
      - 'CHANGELOG.md'
      - '.gitignore'
      - '.gitattributes'
      - 'infection.json.dist'
      - 'psalm.xml'

name: Run tests

jobs:
  tests:
    name: PHP ${{ matrix.php }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        php:
          - '8.1'
          - '8.2'
          - '8.3'

    defaults:
      run:
        working-directory: .

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Generate key
        run: |
          make generate-mongo-key
          sudo chmod 400 ./mongo-key.key
        with:
          name: output-mongo-key
          path: mongo-key.key

      - name: Build
        run: make build v=${{ matrix.php }}

      - name: Debug
        run: docker ps -a

      - name: Container logs
        run: docker logs db-mongodb-db-primary

      - name: Create cluster
        run: make create-cluster-mongodb

      - name: Run tests with phpunit
        run: make test v=${{ matrix.php }}
